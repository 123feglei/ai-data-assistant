# 「问问数据」AI 数据分析求职作品 · 讲解与面试准备

> 适用岗位：AI 数据分析 / 数据分析师（LLM 应用方向）
> 本文档基于本仓库真实实现编写，所有数字来自本地评测与代码，可复现、可深挖。
> 原则：面试时**只讲真实做过的事**，涉及数字给区间并说明口径，不编造。

---

## 一、项目一句话定位

> 用自然语言（中文）问业务数据，系统自动生成 SQL、查询 SQLite 电商数据库，
> 返回「结果表格 + 自动图表 + 一句话业务结论」的端到端对话式数据分析工具。

**技术标签**：Python · NL2SQL（自建 Prompt 链路）· SQLite · DeepSeek LLM · Streamlit · Plotly

**简历关键词**：NL2SQL / Prompt 工程 / 少样本示例（few-shot）/ 只读安全校验 / 失败自动重试 /
离线评测驱动优化 / 结果自动可视化 / 智能结论生成

---

## 二、完整 Web 应用工作模式（讲给面试官听的主线）

### 2.1 总架构

```
┌───────────────────────── 前端（Streamlit，app.py）─────────────────────────┐
│  QuickBI 风格界面：品牌横幅 + 示例问题胶囊 + 侧边栏数据说明                    │
│  每条回答渲染为「结果卡片」：智能洞察高亮条 + Tabs(图表/数据明细/SQL)          │
└──────────────────────────────┬─────────────────────────────────────────────┘
                               │ 用户问题
                               ▼
┌──────────────────── NL2SQL 引擎（nl2sql/engine.py）─────────────────────┐
│ ① 组装 Prompt：schema 摘要 + 8 条 few-shot + 系统口径规则 + 用户问题       │
│ ② 调 DeepSeek（temperature=0）→ 提取 SQL                                 │
│ ③ 只读校验（仅 SELECT / 禁写关键词）                                      │
│ ④ SQLite 只读连接执行（自动 LIMIT 200 + 15s 超时）                        │
│ ⑤ 失败 → 把数据库报错回喂 LLM，修正后重试 1 次                              │
└──────────────────────────────┬─────────────────────────────────────────────┘
                               │ DataFrame
                               ▼
┌──────────── 结果处理（nl2sql/present.py）：自动选图 + 一句话结论 ───────────┐
│  标量→KPI 数字卡 · 时间字段→折线图 · 占比字段→饼图 · 分类+数值→柱状图         │
│  结论：结果前 8 行喂 LLM，强制"必须包含结果中的数字，禁止编造"                │
└──────────────────────────────────────────────────────────────────────────┘
```

### 2.2 代码文件地图（面试被问"项目结构"时按此讲）

| 文件 | 职责 |
|---|---|
| `app.py` | Streamlit 入口：界面、会话历史、把各模块串起来 |
| `nl2sql/config.py` | 统一配置：读 `.env`，DeepSeek / Ollama 双后端切换 |
| `nl2sql/engine.py` | **核心 NL2SQL 引擎**：Prompt 组装、SQL 生成/校验/执行、失败重试 |
| `nl2sql/present.py` | 结果呈现：自动选图 + 一句话结论生成 |
| `evaluate.py` | 离线评测：35 题结果对比、错误分类、Markdown 报告 |
| `scripts/import_data.py` | CSV → SQLite 入库（幂等，可带 `--with-geolocation`） |
| `scripts/cli_query.py` | 命令行测试入口（不开界面也能跑通链路） |
| `data/schema.md` | 8 张表全字段中文说明 + JOIN 键 + 指标口径（**引擎自动解析它**） |
| `data/eval_set.json` | 评测集：35 题 × 5 类，含金标准 SQL |
| `reports/eval_report_*.md` | 4 轮评测报告（留存了 71%→100% 的优化轨迹） |

### 2.3 一次提问的端到端流程（背下来，能讲 3 分钟）

1. **入参**：用户在界面输入"各州 2018 年客单价排名"或点击示例问题。
2. **组装 Prompt**（engine.build_schema_summary + build_prompt）：
   - schema 摘要**不是手写的**，而是运行时解析 `data/schema.md` 的 markdown 表格自动生成
     （字段名/类型/中文含义/JOIN 键/指标口径）——改 schema 文档，Prompt 自动同步，不会两处维护漂移；
   - 拼接 8 条 few-shot（覆盖订单总数、GMV、支付占比、评分分布、品类 TOP、州分布、月度趋势、复购率），
     few-shot 的 SQL **与评测集金标准口径严格一致**（这是准确率高的关键）；
   - 系统指令里写死口径规则与安全约束。
3. **生成**：调 `deepseek-chat`，`temperature=0` 保证稳定输出；只要求输出 SQL。
4. **提取与校验**：`extract_sql` 兼容代码块/裸 SQL 输出；`validate_sql` 只放行 SELECT/WITH，
   用词边界正则拦截 INSERT/UPDATE/DELETE/DROP 等写操作。
5. **执行**：以 SQLite URI **只读模式**打开数据库（数据库层禁写，与第 4 步形成双保险）；
   若 SQL 没写 LIMIT 自动补 `LIMIT 200`；用 `set_progress_handler` 实现 15 秒超时中断
   （跨平台，Windows 没有 signal.SIGALRM 也能用）。
6. **失败重试**：执行抛错（列不存在、语法错等）→ 把报错原文拼进 Prompt 再问一次（共 2 次）。
7. **呈现**：结果 DataFrame 交给 present：单值出蓝色 KPI 大卡；有时间字段出折线；
   有 pct/占比出饼图；分类+数值出柱状（QuickBI 配色）。
8. **结论**：把结果前 8 行喂 LLM，要求"一句话、含具体数字、禁止编造"，
   渲染成卡片顶部的"智能洞察"高亮条。
9. **会话**：Streamlit `session_state` 存历史，提问后 `st.rerun()` 全量重绘；
   每条回复内含 SQL/表格/图表三个 Tab，便于"人审 SQL + 看数据"。

### 2.4 数据底座（怎么来的）

- Kaggle 巴西电商 Olist（真实脱敏交易数据），8 张业务表共 **55 万行**（orders/order_items/customers/
  sellers/order_payments/order_reviews/products/品类翻译），另有可选地理表（体积原因默认不导）。
- 全部字段**按文本原样入库**（邮编前导零、日期字符串都不丢精度），需要日期/数值处理时在 SQL 侧用
  `strftime`/`julianday`/`CAST`——保证与 Kaggle 原始数据一致，评测对标才可信。
- 指标口径写死在 schema.md 第 4 节，且**评测集 gold SQL、few-shot 与口径三者统一**
  （例如"订单量/成交额统计默认排除 canceled 订单"，这是本项目真实踩坑后沉淀的规则，见第七节）。

---

## 三、项目介绍话术

### 3.1 电梯版（30 秒，开场自我介绍用）

> "我做了一个对话式数据分析工具，叫「问问数据」。业务人员用中文问一句
> 『哪个品类卖得最好』，系统自动生成 SQL、查库，返回图表和一句话结论。
> 我用的是**自建 NL2SQL 链路**而不是现成框架：把数据表结构、口径规则、示例问答组装成 Prompt
> 交给 DeepSeek 生成 SQL，再做只读校验和失败重试。我搭了 **35 题的评测集**，
> 三轮优化把准确率从 71% 提到 97%~100%，这个评测闭环是项目最有含金量的部分。"

### 3.2 详细版（90 秒，讲完等追问）

> 分四层讲：
> **1）为什么做**：业务取数依赖 SQL 门槛高，我用 LLM 把自然语言转成 SQL，验证这条路能否端到端走通。
> **2）怎么做**：核心是一个自建 Prompt 工程链路——不是直接甩一句"给我 SQL"，
> 而是喂 schema 摘要 + 8 条示例 + 明确口径规则，让模型照着示例的口径写 SQL；
> 安全上做了正则校验 + 数据库只读双保险，加超时和结果行数限制；
> 写错了就带报错重试一次。
> **3）怎么证明它好用**：这是重点。我建了 35 题覆盖 5 类问题的评测集，
> 每题有金标准 SQL，用「执行结果对比」而非看 SQL 文本判断对错；
> 首轮只有 71%，通过分析失败原因——几乎全是口径不一致而非模型能力问题——
> 统一口径、补示例、优化判定，最后提到接近满分。这个**评测驱动迭代**的方法论
> 比工具本身更能体现数据分析思维。
> **4）产品化**：Streamlit 做了 QuickBI 风格界面，示例问题引导、SQL 可展开自查、
> 自动选图、智能结论，可以一键本地启动。"

### 3.3 简历条目（可放两版，合并用）

**技术版：**
> **对话式数据分析助手（NL2SQL）** · Python / SQLite / DeepSeek / Streamlit
> - 设计自建 Prompt 链路：schema 摘要自动解析 + 8 条 few-shot + 口径规则，LLM 生成 SQL，实现只读校验、失败自动重试、超时与 LIMIT 保护；
> - 搭建 35 题五类评测集，用执行结果对比判定，经 3 轮口径与示例优化 SQL 准确率从 71% 提升至 97%+，平均端到端约 4 秒；
> - 结果自动可视化（折线/柱状/饼图自适应）与一句话结论生成，覆盖 GMV、复购率、客单价等 12 个电商核心指标。

**产品/业务版：**
> 主导 NL2SQL MVP 从 0 到 1（PRD→数据→评测→Demo）：定义 6 类问题边界与 12 项指标口径，
> 面向无 SQL 能力的业务人员，将一次取数从"等分析师排期"变为"提问即得图表结论"。

---

## 四、高频面试问答（技术点 + 参考回答）

### A. 产品与架构类

**Q1：为什么用"自建 Prompt 链路"而不是 Vanna 这类 NL2SQL 框架？**
- 对比维度：可解释性 / 可控性 / 可评测 / 开发成本。
- 参考回答："Vanna 开箱即用，但内部机制是黑盒——它怎么拼 schema、怎么选示例我不可控。
  我选择自建，因为**每一步都透明、能调**：schema 怎么描述、示例选哪些、口径怎么规定，出了问题能定位。
  面试官您后面问我任何一环，我都能讲清楚为什么这样设计。代价是多写几百行代码，但对个人作品来说，
  把原理讲透比'调了个框架'更有价值。"
- 加分点：主动说出"如果后续要支持几十张表、复杂业务，Vanna 或检索式选示例是值得引入的，
  我做的是把地基打明白。"

**Q2：整个系统由哪些模块组成，数据怎么流动？（架构题）**
- 用 2.1 的架构图讲一遍：界面→NL2SQL 引擎→SQLite→present→界面。
- 强调：**每个模块单测可独立调用**（cli_query.py 不开界面也能跑链路），这是工程习惯的体现。

**Q3：你们数据从哪来，为什么用 SQLite 不用 MySQL/大数据？**
- 数据是 Kaggle 巴西电商 Olist 公开数据集，55 万行规模。
- SQLite 单文件零运维，个人作品和本地评测最合适；评测要"结果可复现"，单文件最稳。
- 加分点："如果接入公司级 MySQL/数仓，只要把 `execute_sql` 的连接层替换，
  上层 Prompt 和评测逻辑完全不用动——架构上我把 SQL 方言差异收敛在了一个函数里。"

### B. NL2SQL 核心类（必问，要能逐行讲）

**Q4：Prompt 具体怎么拼的？为什么这么设计？**
- 三层：系统指令（角色 + 规则）→ 用户内容里三段（schema 摘要 + few-shot 示例 + 当前问题）。
- schema 摘要**自动解析**自 `data/schema.md`（markdown 表格→文本），含表字段、JOIN 键、指标口径。
- 设计动机：schema 描述决定了模型"知道有哪些表和字段"，few-shot 决定"按什么口径和写法"，
  系统规则兜底安全与边界。**三者都来自同一份口径定义**，避免模型困惑。
- 加分点：举例说明 few-shot 与 gold SQL 口径必须一致——早期我的 GMV 示例不过滤取消单、
  而评测金标准过滤，模型一会儿学示例一会儿学规则，那题就反复错；统一后就稳定了。

**Q5：怎么防止模型"编造不存在的表/列"（幻觉）？**
- 三道防线：
  1) Prompt 明令"表名列名必须来自 schema，禁止臆造"；
  2) 可执行校验兜底：列不存在 → SQLite 报 `no such column` → 报错回喂模型自动修正；
  3) 评测集把"表名幻觉/列名幻觉"作为错误分类维度，靠评测量化这类错误占比再针对性补 schema 描述。
- 加分点：讲"这类问题靠 Prompt 堵不如靠'执行验证+报错重试'收口"——工程上把不可控的生成
  变成可控的闭环。

**Q6：生成 SQL 错了怎么办？（必须有答案）**
- 自动重试 1 次：捕获执行异常 → 把数据库原始报错拼进 Prompt（"上次 SQL 执行失败：xxx，请修正"）
  → 重新生成 → 再校验执行。仍失败则把 SQL 展示给用户自查 + 友好提示"换个问法"。
- 评测统计：35 题平均 LLM 调用 1.00 次/题，说明多数一次就对，重试是兜底不是常态。

**Q7：为什么 temperature=0？**
- SQL 生成是**确定性任务**，需要稳定可复现；结论生成才用 0.3 让语言自然一点。
- 加分点：说明"temperature 是任务类型决定的，不是一个值走天下"。

### C. 安全与执行类

**Q8：怎么保证 SQL 安全？万一模型输出 DELETE 呢？**
- 四层：① 系统 Prompt 约束只写 SELECT；② 代码层正则校验首个关键字必须是 SELECT/WITH，
  并拦截 INSERT/UPDATE/DELETE/DROP 等关键词（词边界匹配，防止误伤列名）；③ SQLite 用
  URI **只读模式**打开（`mode=ro`），即使前两层被绕过，数据库层面也写不进去；④ 自动补 LIMIT 防拖垮库。
- 加分点：强调"**校验层 + 数据库层双保险**，安全不依赖模型的自觉"。

**Q9：SQLite 怎么做超时？Windows 没有 SIGALRM 吧？**
- 用 `sqlite3.connect` 的 `set_progress_handler(callback, n)`：每执行 n 条 SQLite 虚拟机指令回调一次，
  回调里检查累计耗时 >15s 就返回非 0 中断查询——纯 SQLite 原生机制，跨平台。
- 加分点：顺带提一句"如果是 MySQL/PG，用 `statement_timeout` / JDBC 超时即可，SQLite 单机场景这是通用做法"。

**Q10：为什么要"结果 LIMIT 200"？**
- 防模型写 `SELECT * FROM order_items`（11 万行）直接拖垮页面/前端渲染；
  业务演示看前 200 行足够，评测里也保证了可比性。

### D. 评测类（重点加分区，面试必讲）

**Q11：准确率是怎么算的？只看 SQL 文本对不对吗？**
- **不是文本比对**，是"执行结果比对"：分别执行模型 SQL 和金标准 SQL，
  把两个结果集规范化后比较（数值 round 到 2 位防浮点误判、行排序后比较、忽略列名顺序）。
- 我特意做"宽容但不放水"的处理：模型列别名不同（如 gmv vs total）允许列排列匹配；
  多输出了辅助列（如同时给了 total_orders）允许子集匹配——因为业务上答案是对的。
- 指标集：总准确率、5 类分准确率、执行成功率、平均耗时、错误类型分布。

**Q12：35 题够吗？评测集怎么设计的？**
- 参考 NL2SQL 常见分类，按 5 类设计：简单单表(8)/聚合统计(9)/多表JOIN(8)/时间范围(5)/复杂组合(5)。
- 每题三件套：自然问法 + 金标准 SQL + 类别标签；gold SQL 全部先本地跑通再进评测集。
- 诚实说局限："35 题是 MVP 量级，主要用于**量化自己的迭代效果**；
  如果要严谨对标业界，应该扩展到百题级并用 Spider/BIRD 这类公开基准，这是我项目里明确标注的下一步。"

**Q13：讲讲 71%→100% 的优化过程（重点！面试官最爱听）**
- 第一轮基线 71.4%：few-shot 只有 4 条，口径规则一刀切"统计默认排除取消订单"。
- 失败分析（关键：几乎所有错都是"口径不一致"，不是模型不会写 SQL）：
  ① 模型对"总数"和"成交额"到底要不要排除取消单判断混乱；
  ② GMV 的 few-shot 示例没过滤取消单，但评测金标准过滤了——**示例和金标准打架**；
  ③ 支付占比这类模型去 JOIN orders 过滤，跟"明细分布不过滤"的口径冲突；
  ④ 数值 round 精度、额外辅助列导致"结果对但被判错"。
- 对应修复：把口径写成"订单量/成交额类 JOIN orders 并排除 canceled；支付/评价等明细直接查原表；
  客户数按 customer_unique_id 去重"，few-shot 扩到 8 条并与 gold 严格对齐；
  判定放宽数值精度与别名/辅助列。→ 97.1% → 100%。
- 升华句："优化 SQL 准确率的瓶颈不在模型，在于**把业务口径定义清楚并让示例、规则、评测三者一致**，
  这和做数据分析'指标口径对齐'是同一件事。"（这句很能打动数据分析岗面试官）

**Q14：评测会消耗 API 成本吗？怎么控制？**
- 35 题 × 最多重试 1 次 ≈ ≤70 次调用/轮；DeepSeek 单价低，整轮开发评测总成本在几元量级。
- 加分点：体现成本意识（对应 PRD 里的成本约束设计）。

### E. 数据与口径类

**Q15：客单价、GMV 这些口径你当时怎么定的？踩过什么坑？**
- 口径写进 schema.md 第 4 节并作为 Prompt 的一部分。GMV=Σ order_items.price（不含运费），
  复购率=按 customer_unique_id 统计购买≥2 次客户占比，配送时长=送达−下单的 julian 天数差。
- 坑：Olist 的 customers 表是"每订单一行"，直接 COUNT 会把人头数算成订单数——
  所以"客户数"必须对 customer_unique_id 去重；这种"表结构≠业务语义"的坑最有分享价值。

**Q16：为什么不用 pandas 算指标，而要模型写 SQL？**
- 这正是产品价值：业务人员不懂 pandas 也不懂 SQL，他们只需要"提问"；
  SQL 对数据库透明、可复用、可审计，展示给用户自查也建立信任。

### F. 界面与工程类

**Q17：Streamlit 界面哪些细节体现了产品思维？**
- 示例问题可点击（降低空输入门槛）；SQL 折叠/Tab 化（**可审计、可复现**，不黑盒）；
- 结果"单值出 KPI 大卡、时间出折线、占比出饼图"自动选图；一句话结论 + 数据支撑并存；
- 侧边栏写清"能问什么/边界在哪"，把超出范围的问题（预测、无关话题）在 Prompt 和文案层面引导掉。

**Q18：会话历史是怎么实现的？每次刷新不丢吗？**
- 用 Streamlit `session_state` 存消息列表；提问后追加并 `st.rerun()` 重绘。
- 诚实补充："刷新页面会清空，因为没做持久化存储；要上线成产品可接 Redis/数据库。这是 MVP 取舍。"

**Q19：为什么数据字段都按文本存？**
- 保住邮编前导零、日期格式、浮点精度，让 SQLite 行为与 Kaggle 原始 CSV 完全一致；
  需要数值/日期计算时在 SQL 里 CAST/strftime。代价是查询时稍慢，55 万行规模可忽略。

**Q20：有没有碰到过印象深刻的坑？**
真实排错案例（挑 1-2 个讲，展示 debug 能力）：
1. **环境坑**：本地 numpy 升到 2.x 后旧版 pyarrow 直接 import 崩溃（`np.unicode_` 被移除），
   `st.dataframe` 全挂——升级 pyarrow 解决；这解释了为什么部署要用干净环境装依赖。
2. **解析坑**：schema 摘要自动解析器首版漏掉"表间关联"段——schema.md 里那节是二级标题 `##`，
   我正则匹配成了三级 `###`，导致模型拿不到 JOIN 键。加了个断言单测就抓到了。
3. **口径坑**：如 Q13，few-shot 与金标准打架导致 GMV 题反复错。

### G. 局限与下一步（防止被问住）

**Q21：这个项目有什么不足？如果继续做会怎么改？**
诚实清单（每条带改进方向）：
1. 评测集规模小（35 题）→ 扩到百题，引入 Spider/BIRD 对标；
2. 只支持单库 8 表 → 抽象连接层支持 MySQL/数仓；
3. 无多轮记忆 → 引入会话上下文与澄清反问（"您说的'上个月'是指自然月吗"）；
4. 无检索式示例选择（全部 8 条都塞给模型）→ 表多后按问题检索最相关 few-shot（减少 token 提升命中）；
5. 结论依赖 LLM → 可加"结论数字与表格一致性校验"，防止模型引用错误数字。

---

## 五、Demo 演示脚本（建议 3 分钟）

| 步骤 | 操作 | 讲解要点 |
|---|---|---|
| 1 | 打开首页 | 一句话定位；指侧边栏"数据集/能问什么/边界"说明产品边界感 |
| 2 | 点击"总 GMV" | 演示单值 → KPI 大卡；展开 SQL Tab 说"SQL 可自查，不黑盒" |
| 3 | 点击"各州订单分布" | 演示 JOIN + 分组 → 柱状图自动出现；强调"自动选图规则" |
| 4 | 点击"每月订单量趋势" | 演示时间字段 → 折线图 |
| 5 | 点"支付方式占比" | 演示饼图；读一遍智能洞察，强调结论含真实数字 |
| 6 | 输入一句没见过的："2018 年取消率最高的州" | 现场验证泛化能力（不在示例里也能答） |
| 7 | （可选）故意问一句超出范围的 | 演示友好错误提示 |
| 8 | 收尾 | 一句话讲评测：35 题三轮 71%→97%+，并说"优化靠的是口径统一与评测驱动" |

---

## 六、写进简历/自我介绍的关键数字（全部真实）

- 8 张表 55 万行真实电商数据，SQLite 单文件 65MB
- 35 题 × 5 类评测集；三轮优化 SQL 准确率 71% → 97%~100%（多轮实测区间）
- 平均端到端约 4 秒/题（多数 <6s），执行成功率 100%
- 12 个核心业务指标口径（GMV/客单价/复购率/各州分布/品类TOP/支付分布/月趋势/评分分布/配送时长等）
- API 成本：全流程开发评测总成本约几元（DeepSeek）

> 口径提醒：面试若被追问"准确率为什么不是固定 100%"，答——
> "temperature=0 下模型仍可能有小概率波动，35 题多轮实测在 97%~100%；
> 我在报告里如实记录每轮数据，不写成单次最好的 100%。"

---

## 七、项目真实踩坑复盘（D1-D5 教训，体现分析思维）

1. **数据导入比想象费时间** → 用脚本幂等化（import_data.py），一行命令可重建数据库。
2. **SQLite 把字段全按文本存** → 保住精度与格式，代价小收益大。
3. **指标口径是第一矛盾源** → 评测里 90% 的"错题"是口径不一致，不是模型笨；
   最终把口径固化为 schema.md 第 4 节，让 Prompt/示例/评测三点一线。
4. **few-shot 必须与金标准同口径**，否则模型"学示例还是听规则"会左右横跳。
5. **结果对比判定要宽容别名与辅助列、容忍浮点精度**，否则会误杀正确答案。
6. **解析器、正则这类代码必须有断言测试**（schema 摘要解析、SQL 校验都加了单测）。
7. **界面好看不等于能跑**：pyarrow/numpy 版本、Streamlit API 变更（use_container_width 弃用）
   这类环境坑要敢于升级依赖解决。

---

## 八、面试前 30 分钟自查清单

- [ ] 能不看代码画出架构图并讲数据流
- [ ] 能逐行讲 `engine.answer` 的 try 重试逻辑
- [ ] 能说出 build_schema_summary 为什么不用手写 schema
- [ ] 能讲清评测"执行结果对比"判定的三个宽容点
- [ ] 能把 71%→100% 的失败归因和修复讲成故事
- [ ] 能主动说出 3 个以上项目局限及改进方案
- [ ] 本地 `streamlit run app.py` 能在面试前现场跑通

---

## 九、理解代码运行逻辑：文件阅读路线（自学 / 给面试官讲代码）

不要从头到尾逐行读。按「先事实 → 再机制 → 后验证 → 最后界面」的顺序，
每一层只回答一个问题，自然形成闭环。

**第 1 步：先读两份"事实文档"，不读代码**
- `data/schema.md`：库里有哪 8 张表、字段、JOIN 键、**指标口径**；
- `data/metrics.md`：项目要支撑的 12 个业务指标长什么样（业务目标）。
- 目的：先建立"业务世界"，再进"代码世界"。

**第 2 步：数据从哪来 → `scripts/import_data.py`**
- 回答：CSV 怎么变成 `data/ecommerce.db`？字段为何按文本原样入库？

**第 3 步：核心引擎 → `nl2sql/engine.py`（最重要，按调用关系从上往下读）**
- `build_schema_summary()` → schema.md 如何被自动解析为 Prompt 摘要（schema 文档即真相源）；
- `build_prompt()` → Prompt = schema 摘要 + few-shot 示例 + 用户问题；
- `validate_sql()` → 只读安全校验；
- `execute_sql()` → 只读连接 + 超时 + LIMIT；
- `answer()` → 串联前四步的主流程（失败→重试 1 次），从它倒着读更容易懂。
- 前置：`nl2sql/config.py` 只花 1 分钟（API Key 来源、DeepSeek/Ollama 切换）。

**第 4 步：怎么证明它好用 → `evaluate.py` + `data/eval_set.json`**
- 评测集 35 题 × 5 类 + 金标准 SQL 的结构；
- `frames_equivalent()` 为什么用"执行结果对比"而非 SQL 文本对比（别名/辅助列/精度三个宽容点）。

**第 5 步：界面怎么串起来 → `app.py`（忽略 CSS，只看调用关系）**
```
用户提问 → ask() → engine.answer() 拿 df → present.generate_conclusion() 拿结论
                  → build_figure() 自动选图 → render_assistant() 渲染卡片/Tabs
历史 → session_state.messages → 每次 rerun 全量重绘
```

**闭环解释（两种视角）**

视角 A：一次提问的运行时闭环（纵向）
```
业务问题 → Prompt(schema+示例+规则) → DeepSeek 生成 SQL
        → 只读校验 → SQLite 执行 → 失败带报错重试1次
        → DataFrame → 自动选图 + 一句话结论 → 界面展示(SQL 可展开自查)
```
把 `engine.answer()` 当作环心，向两端延伸即可讲通。

视角 B：开发期的"质量飞轮"闭环（横向，最能体现数据分析思维）
```
metrics.md(业务指标) → eval_set.json(金标准) → engine 的 few-shot/schema 口径
        → 生成 SQL → evaluate.py 结果对比 → 报告(71%→97%+) ──┐
        ↑                                                        │
        └────── 根据错误类型反哺口径文档与示例 ←──────────────────┘
```
闭环核心一句话：**口径文档 → 示例/规则 → 生成 → 评测 → 反哺口径**。
准确率提升不是靠调模型，而是靠把这个环转起来。

**读完跑一遍，胜过再读一遍：**
```powershell
python scripts/cli_query.py "各州 2018 年客单价排名？"  # 不开界面看完整链路
python evaluate.py --limit 5                            # 看评测如何逐题判对错
python -m streamlit run app.py                          # 用界面把一切可视化
```

---

*本文件与代码仓库保持同步；面试回答请以真实实现为准，不确定的数字先查代码再开口。*
