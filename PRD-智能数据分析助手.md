# PRD：智能数据分析助手「问问数据」（NL2SQL MVP）

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 作者 | 待填（你的名字） |
| 开发方式 | Trae（AI 编程 IDE）辅助开发 |
| 工期 | 7 天（一周） |
| 项目定位 | 个人学习 + 求职作品（AI数据分析 / AI产品经理方向） |
| 文档用途 | ① 指导开发 ② 喂给 Trae 的需求依据 ③ 简历与面试素材 |

> 一句话产品定义：**用户用自然语言提问业务数据，系统自动生成 SQL、执行查询、输出图表和文字结论的对话式数据分析工具（MVP）。**

---

## 1. 项目背景与目标

### 1.1 背景
- 业务人员取数高度依赖 SQL / 数据分析师，门槛高、周期长。
- 大模型（LLM）已具备将自然语言转换为 SQL（NL2SQL）的能力，可大幅降低取数门槛。
- 本项目的直接目的：**在 7 天内做出一个可演示、可复现的端到端作品**，用于求职简历展示，并在过程中掌握 LLM 应用开发全流程。

### 1.2 产品目标
- 用户输入中文/英文自然语言问题 → 得到「SQL + 结果表格 + 图表 + 一句话结论」。

### 1.3 成功指标（可量化，写进简历）
| 指标 | 目标值 |
|---|---|
| 评测集 SQL 生成准确率 | ≥ 80% |
| 单条查询端到端耗时 | ≤ 10 秒 |
| 支持的问题类型 | ≥ 6 类（单表查询/聚合统计/多表JOIN/时间范围/排序TOP/比较） |
| 覆盖核心业务指标 | ≥ 10 个（GMV、订单量、客单价、复购率、各州分布、品类TOP、支付方式、月度趋势、评分分布等） |
| 演示 Demo | 可本地一键启动 |

### 1.4 非目标（明确不做，防止范围蔓延）
- ❌ 多轮对话记忆与复杂 Agent 编排
- ❌ 用户注册/权限系统
- ❌ 多数据源接入、数据同步
- ❌ 模型微调（用 Prompt 工程 + few-shot 即可）
- ❌ 分布式/大数据框架（Spark 等）
- ❌ 生产级部署（云服务器、监控告警）

---

## 2. 目标用户与使用场景

### 2.1 目标用户
- **主用户（学习/演示对象）**：没有 SQL 能力的业务分析师、运营、产品经理。
- **次用户（作品读者）**：求职面试官、简历审阅者。

### 2.2 用户故事
| 编号 | 用户故事 | 对应能力 |
|---|---|---|
| US-01 | 作为业务分析师，我想问"上个月各州的 GMV 是多少"，以获得表格和柱状图 | 单表聚合 + 分组 |
| US-02 | 作为运营，我想问"哪个品类的复购率最高"，以获得排名 | 多表 JOIN + 聚合 |
| US-03 | 作为管理者，我想问"过去 6 个月的订单量趋势"，以获得折线图 | 时间范围 + 趋势 |
| US-04 | 作为用户，我问了超出数据范围的问题，希望能得到友好提示而不是报错 | 错误处理 |
| US-05 | 作为新用户，我希望页面提供示例问题，知道怎么用 | 示例引导 |

---

## 3. 产品范围（MVP）

### 3.1 做（In Scope）
1. 单业务库（SQLite）多表自然语言查询
2. LLM 生成 SQL + 执行 + 结果校验 + 失败自动重试（1 次）
3. 结果以表格 + 图表展示（plotly）
4. 生成一句话自然语言结论
5. 友好错误提示（含"当前数据不支持/换个问法"）
6. 离线评测脚本 + 评测集（30~50 题）
7. Streamlit 中文对话界面 + 示例问题
8. README（架构说明 + 快速开始）+ Demo 录屏

### 3.2 不做（Out of Scope）
见 1.4 非目标。

---

## 4. 功能需求

> 每条功能含验收标准，Trae 实现后按此验收。

### FR-01 自然语言 → SQL 生成（核心）
- **描述**：用户输入自然语言问题，系统基于数据库 schema 和 few-shot 示例，调用 LLM 生成 SQL。
- **输入**：用户问题文本。
- **输出**：一条可执行的 SELECT SQL。
- **约束**：只允许 SELECT；自动附加 LIMIT（默认 200，可配置）。
- **验收标准**：
  1. 10 条基础问题中 ≥ 8 条生成可执行 SQL；
  2. 生成的 SQL 表名/列名必须来自真实 schema（不允许幻觉列）；
  3. 无 SQL 时给出明确错误提示。

### FR-02 SQL 执行与安全校验
- **描述**：对生成的 SQL 做只读校验（正则/关键词检查禁止非 SELECT 语句），在 SQLite 中执行，超时保护（如 15 秒）。
- **验收标准**：
  1. 恶意/非法 SQL（DELETE、DROP 等）被拦截并提示；
  2. 执行异常（列不存在、语法错误）时捕获并返回错误信息给 FR-05 重试；
  3. 返回结果带行数和耗时。

### FR-03 结果表格与图表展示
- **描述**：查询结果渲染为表格；根据字段特征自动选择图表（时间字段→折线图；分类+数值→柱状图；占比→饼图）。
- **验收标准**：
  1. 表格正确显示；
  2. 时间序列问题自动出折线图，分组问题自动出柱状图；
  3. 图表无数据时有占位提示。

### FR-04 自然语言结论生成
- **描述**：基于查询结果（关键数字）调用 LLM 生成一句话结论。
- **验收标准**：结论包含具体数字（如"2023 年 3 月 GMV 为 120 万元，环比增长 8%"），不编造数据。

### FR-05 错误处理与自动重试
- **描述**：SQL 执行失败时，把数据库错误信息回传给 LLM，要求修正 SQL 后重试（最多 1 次）；仍失败则给出友好提示并展示已生成的 SQL 供用户自查。
- **验收标准**：错误重试链路生效；最终失败时提示文案友好且包含建议。

### FR-06 评测集与离线评测（技术亮点，面试必讲）
- **描述**：提供评测脚本 `evaluate.py`，对评测集中每题运行 NL2SQL 链路，与金标准 SQL 结果对比，输出准确率、失败明细、错误类型分布。
- **验收标准**：
  1. 评测集 30~50 题，覆盖 5 类问题；
  2. 输出报告包含：总准确率、各类问题准确率、平均耗时、错误明细；
  3. 报告可导出为 Markdown。

### FR-07 对话界面（Streamlit）
- **描述**：中文界面，包含输入框、示例问题按钮、历史对话列表、SQL 折叠展示、图表区、结论区。
- **验收标准**：
  1. `streamlit run app.py` 一键启动；
  2. 提问→出 SQL→出图→出结论全流程无人工干预；
  3. 页面为中文。

### FR-08 示例问题引导
- **描述**：页面顶部展示 4~6 个可点击的示例问题，点击后自动提问。
- **验收标准**：点击示例问题可正常触发完整流程。

---

## 5. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 首次回答 ≤ 15 秒，平均 ≤ 10 秒（API 网络正常时） |
| 可用性 | 界面全中文；错误提示可理解 |
| 可复现 | `requirements.txt` + `README` 快速开始步骤，新机器 30 分钟内可跑通 |
| 成本 | 开发期 LLM 调用量控制在每日 ≤ 300 次（DeepSeek API 总成本约几元）；如无 API Key 可切换本地 Ollama 模型 |
| 兼容 | Windows / macOS 本机可运行；Python 3.10+ |

---

## 6. 数据需求

### 6.1 数据源
- **首选**：巴西电商 Olist 公开数据集（Kaggle，免费）：https://www.kaggle.com/datasets/olistbr/brazilian-ecommerce
  - 共 8 张表：`orders`（订单）、`order_items`（订单明细）、`customers`（客户）、`sellers`（卖家）、`order_payments`（支付）、`order_reviews`（评价）、`products`（商品）、`product_category_name_translation`（品类翻译）
- **备选**：Chinook 音乐商店数据库（更轻量）：https://github.com/lerocha/chinook-database

### 6.2 数据落地
- 导入 SQLite 单文件数据库 `data/ecommerce.db`
- 提供 `data/schema.md`：每张表的字段说明 + 字段含义注释（中文），供 NL2SQL 使用
- 提供 `data/sample_queries.md`：5~10 条"标准问法 → 金标准 SQL"示例（few-shot 素材）

### 6.3 核心业务指标清单（≥10 个，作为示例问题和评测集来源）
1. GMV（订单总额）
2. 订单量
3. 客单价（GMV/订单数）
4. 复购率（≥2 次购买客户占比）
5. 各州订单分布
6. 品类销量 TOP10
7. 支付方式分布
8. 月度订单量趋势
9. 订单评分分布
10. 平均配送时长
11. 高价值客户（TOP10 消费客户）
12. 季度 GMV 对比

---

## 7. 技术方案

### 7.1 总体架构（文字图）
```
Streamlit 界面 (app.py)
      │  用户问题
      ▼
NL2SQL 引擎 (nl2sql/engine.py)
  ├─ 1. 组装 Prompt：schema 摘要 + few-shot 示例 + 用户问题
  ├─ 2. 调用 LLM（DeepSeek API / Ollama 本地模型）→ SQL
  ├─ 3. 校验只读 → SQLite 执行
  └─ 4. 失败则带错误信息重试 1 次
      │  结果 DataFrame
      ▼
结果处理 (nl2sql/present.py)：表格 + 自动选图 + 结论生成
      ▼
Streamlit 渲染：图表(plotly) + 结论 + SQL 折叠展示
```

### 7.2 技术栈
| 层 | 选型 | 说明 |
|---|---|---|
| 语言 | Python 3.10+ | |
| 数据库 | SQLite | 零配置，单文件 |
| 界面 | Streamlit | 最快出可交互 Demo |
| 图表 | plotly | 交互式图表 |
| LLM | **首选 DeepSeek API**（openai SDK 兼容，便宜、中文好）；备选 Ollama 本地 qwen2.5:7b | |
| NL2SQL 实现 | **自建 Prompt 链路（推荐，面试能讲清楚原理）**；备选 Vanna 框架 | 见 7.3 |
| 依赖管理 | requirements.txt + venv | |

### 7.3 NL2SQL 方案选型（关键决策）
- **方案 A（推荐）：自建 Prompt 链路**
  - Prompt 组成：系统指令 + 数据表 schema 描述 + few-shot 示例（3~5 条）+ 用户问题
  - 优点：原理透明、可评测、可优化、面试能讲清"为什么这样设计"
- **方案 B：Vanna 开源框架**
  - 优点：开箱即用；缺点：内部机制黑盒，面试难展开
- **决策：主用方案 A**。若 D3 进度落后，可临时切换到方案 B 保底。

---

## 8. 界面与交互

### 8.1 页面布局
```
┌─────────────────────────────────────────────┐
│  📊 问问数据：对话式数据分析助手          │
│  示例问题：[上个月GMV] [复购率TOP品类] [月趋势] │
├─────────────────────────────────────────────┤
│  输入框: [________________________] [提问]  │
│  历史对话:                                   │
│   Q: 哪个品类复购率最高？                   │
│   ├─ SQL (折叠)  SELECT ...                 │
│   ├─ 📈 柱状图                              │
│   └─ 💬 结论: "家居用品复购率最高，为 8.2%" │
└─────────────────────────────────────────────┘
```

### 8.2 交互流程
1. 用户提问（或点击示例问题）
2. 页面显示"分析中…"
3. 依次渲染：SQL（折叠）→ 结果表格 → 图表 → 结论
4. 失败时显示友好错误 + 建议

---

## 9. 评测方案（面试故事的核心素材）

### 9.1 评测集构成（30~50 题）
| 类别 | 题数 | 示例 |
|---|---|---|
| 简单单表查询 | 10 | "一共有多少订单？" |
| 聚合统计 | 10 | "各支付方式的订单占比？" |
| 多表 JOIN | 10 | "哪个品类销量最高？" |
| 时间范围 | 5 | "2023 年 1 月订单量是多少？" |
| 复杂组合 | 5~10 | "各州 2023 年客单价排名前 5" |

### 9.2 评测指标
- **SQL 正确率**：生成 SQL 与金标准 SQL 结果对比（行数一致 + 关键数值近似）
- **执行成功率**：能成功执行的比例
- **平均耗时**
- **错误类型分布**：表名幻觉 / 列名幻觉 / JOIN 错误 / 聚合错误 / 语义误解

### 9.3 优化动作（按错误类型）
| 错误类型 | 优化动作 |
|---|---|
| 表名/列名幻觉 | 细化 schema 描述；在 Prompt 中强调"只能使用给定字段" |
| JOIN 错误 | 在 schema 中写明表间关联键（如 orders.order_id = order_items.order_id） |
| 聚合错误 | 增加对应类型的 few-shot 示例 |
| 语义误解 | 扩充示例问法；必要时在系统指令中给指标口径定义 |

---

## 10. 里程碑与排期（7 天）

| 天 | 任务 | 交付物 | 当日验收标准 |
|---|---|---|---|
| D1 | 环境搭建 + 数据入库 + 写 10 条金标准 SQL | 项目骨架、`data/ecommerce.db`、`data/sample_queries.md` | SQLite 中可查询 8 张表；10 条金标准 SQL 全部可执行 |
| D2 | EDA + 指标清单 | `notebooks/eda.ipynb`、`data/schema.md`、指标清单 | schema.md 覆盖全部字段中文说明；指标清单 ≥ 10 个且可用 SQL 算出 |
| D3 | NL2SQL 链路跑通 | `nl2sql/engine.py`、CLI 测试 | 3 个案例问题端到端输出正确 SQL |
| D4 | 评测 + 优化 | 评测集 30~50 题、`evaluate.py`、评测报告 | 准确率 ≥ 60% 并记录错误类型（D4 目标是从 60% 往 80% 优化） |
| D5 | Streamlit 界面 | `app.py` | 一键启动，完整交互流程可演示 |
| D6 | 产品化包装 | Demo 录屏、PRD 更新、竞品分析（PM 方向） | Demo 视频 2~3 分钟可讲完整流程 |
| D7 | README + 简历 + 演练 | README、简历 bullet、面试话术 | 新机器按 README 30 分钟跑通；能脱稿讲 3 分钟项目 |

> 弹性规则：D4 是质量关键日，若进度紧张优先砍 D6 产品化内容，不砍评测。

---

## 11. 总验收标准（完成即达标）

- [ ] `git clone` 后按 README 30 分钟内本地跑通
- [ ] 评测集准确率 ≥ 80%，评测报告可导出
- [ ] 覆盖 ≥ 10 个核心业务指标、≥ 6 类问题
- [ ] 支持中文提问，错误提示友好
- [ ] 所有代码本人通读一遍并能讲解每一部分作用
- [ ] 简历 bullet 完成（含量化数据）
- [ ] Demo 录屏完成（2~3 分钟）

---

## 12. 交付物清单

1. GitHub 仓库（代码 + 数据说明 + README）
2. `data/ecommerce.db` + `schema.md` + `sample_queries.md`
3. `nl2sql/` 引擎模块 + `evaluate.py` 评测脚本 + 评测报告
4. `app.py` Streamlit 应用
5. Demo 录屏（MP4/GIF）
6. 本 PRD（v1.0）+ 竞品分析（PM 方向）
7. 简历项目条目（两方向模板见附录 B）

---

## 13. 风险与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| Olist 数据导入/理解耗时 | 中 | 限时半天；超时切 Chinook 备选 |
| LLM 生成 SQL 不稳定 | 高 | few-shot 扩充 + 错误重试 + 评测驱动优化 |
| 一周工期紧张 | 高 | 严格执行 3.1 范围；砍 D6 也不砍核心链路 |
| API 成本/Key 问题 | 低 | DeepSeek 充值 10 元足够；无 Key 则用 Ollama 本地模型 |
| 本地环境问题 | 中 | 用 venv 隔离；README 写清 Python 版本与依赖 |

---

## 附录 A：Trae 开发指引（重要，开发前必读）

### A.1 前置准备（0.5 小时）
1. 安装 Python 3.10+（勾选 Add to PATH）
2. 安装 Trae（https://www.trae.ai/）
3. 安装 Git；注册 GitHub 账号
4. 注册 DeepSeek 开放平台（https://platform.deepseek.com/）充值 10 元，创建 API Key

### A.2 在 Trae 中开始项目
1. 打开 Trae → 新建文件夹 `ai-data-assistant` → 打开终端初始化 Git 仓库
2. 把本 PRD 文件放入项目根目录
3. 在 Trae 对话框里输入（可直接复制）：

```
请阅读项目根目录的 PRD-智能数据分析助手.md。
这是我们的产品需求文档。请按第 10 章里程碑，从 D1 开始逐步实现。
现在请先帮我完成 D1：搭建项目骨架、把 Olist 数据导入 SQLite，
并给出 10 条金标准 SQL 写入 data/sample_queries.md。
请同时创建 requirements.txt 和项目目录结构。
```

### A.3 每天的标准工作流
1. 先让 Trae 读 PRD 对应章节（`请阅读 PRD 第 10 章 D4 任务`）
2. 明确当天任务 + 验收标准
3. Trae 写代码 → 你运行验证 → 把报错原样贴回给 Trae
4. 让 Trae 解释关键代码（`请逐行解释 engine.py 的 prompt 组装逻辑`）——**这一步保证面试能讲**
5. 当天收尾：让 Trae 更新 README 进度记录

### A.4 高频提示词模板
- 实现功能：`按 FR-0X 实现 XX 功能，验收标准是：…，请给出完整代码`
- 修 Bug：`运行报错如下：[粘贴报错]。请分析原因并修复`
- 写评测：`请基于 data/schema.md 和 sample_queries.md，为评测集补充 10 道"多表 JOIN"类题目及金标准 SQL`
- 写简历：`请根据 PRD 成功指标和评测报告，把本项目写成简历项目条目，要求量化、两版（技术版/产品版）`
- 写 README：`请为项目写 README：架构图、快速开始、评测结果、截图占位说明`

### A.5 使用 Trae 的 5 条铁律
1. **一次只做一个里程碑**，不要让 Trae 一口气写完所有功能
2. **每段代码必须自己通读**，看不懂就让它解释——面试会问
3. **报错原样粘贴**，不要自己改写（改写会丢失关键信息）
4. **让 Trae 先给方案再写码**：涉及架构决策时先问"你打算怎么实现，为什么"
5. **本地能跑再验收**：Trae 说"完成了"不算数，`streamlit run app.py` 跑通才算

---

## 附录 B：简历素材模板

### B.1 AI数据分析方向
> **智能数据分析助手（NL2SQL + LLM）** | Python · Streamlit · DeepSeek API
> 构建自然语言数据查询系统：基于 schema 摘要 + few-shot 提示工程调用大模型生成 SQL，设计只读校验与失败重试机制。搭建 50 条评测集，SQL 准确率从 58% 优化至 92%，单条查询平均 3 秒，覆盖 12 个核心业务指标，替代人工取数效率提升约 80%。完整实现 EDA → 建模 → 评测 → 部署全链路。

### B.2 AI产品经理方向
> **主导"对话式数据分析"MVP 从 0 到 1** | PRD · 竞品分析 · Demo
> 完成产品定位、用户故事与 MVP 范围定义，产出 PRD 与竞品分析（对比 ChatGPT、传统 BI）。产品支持 6 类分析问题、12 个业务指标，Demo 查询成功率 92%，单次取数由 30 分钟降至 3 分钟。输出 Demo 录屏与演示脚本，可完整讲解从需求到落地的产品闭环。

### B.3 面试可能被问 & 准备要点
| 问题 | 准备要点 |
|---|---|
| 为什么用自建 Prompt 而不是 Vanna？ | 讲对比与取舍（透明性 vs 效率） |
| LLM 生成 SQL 错了怎么办？ | 讲重试机制 + 评测驱动的 few-shot 优化 |
| 怎么保证 SQL 安全？ | 只读校验 + LIMIT + 超时 |
| 准确率怎么算的？ | 金标准 SQL 结果对比，讲评测集设计 |
| 如果让你做产品经理怎么迭代这个产品？ | 讲用户分层、指标（留存/成功率）、商业化 |

---

*（本 PRD 由 AI 辅助整理，开发过程中如有需求变化，请更新版本号并记录变更。）*
