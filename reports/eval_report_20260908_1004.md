# NL2SQL 评测报告

- 评测时间：2026-09-08 10:04:03
- 评测题目：35 题
- 模型：deepseek-chat（temperature=0，失败自动重试 1 次）

## 总览

| 指标 | 数值 |
|---|---|
| 总准确率 | 82.9% (29/35) |
| 执行成功率 | 35/35 (100.0%) |
| 平均端到端耗时 | 5.86s/题 |
| 平均 LLM 调用次数 | 1.00 次/题 |

## 各类别准确率

| 类别 | 题数 | 通过 | 准确率 |
|---|---|---|---|
| 简单单表查询 | 8 | 6 | 75.0% |
| 聚合统计 | 9 | 7 | 77.8% |
| 多表JOIN | 8 | 8 | 100.0% |
| 时间范围 | 5 | 3 | 60.0% |
| 复杂组合 | 5 | 5 | 100.0% |

## 错误类型分布

| 错误类型 | 数量 |
|---|---|
| 结果不一致(语义/聚合差异,需人工复核) | 6 |

## 失败明细（供人工复核与错误类型细分）

### 2. [简单单表查询] 一共有多少位客户？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 2.41s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT COUNT(*) AS customer_count
FROM customers;
```

**金标准 SQL：**

```sql
SELECT COUNT(DISTINCT customer_unique_id) AS customer_count
FROM customers;
```

### 8. [简单单表查询] 平均每件商品的成交单价是多少？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 27.88s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT ROUND(AVG(oi.price), 2) AS avg_price_per_item
FROM order_items oi
JOIN orders o ON oi.order_id = o.order_id
WHERE o.order_status != 'canceled';
```

**金标准 SQL：**

```sql
SELECT ROUND(AVG(price), 2) AS avg_price
FROM order_items;
```

### 11. [聚合统计] 各支付方式的订单占比是多少？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 2.73s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT payment_type,
       COUNT(DISTINCT order_id) AS order_count,
       ROUND(COUNT(DISTINCT order_id) * 100.0 / SUM(COUNT(DISTINCT order_id)) OVER (), 2) AS order_pct
FROM order_payments
GROUP BY payment_type
ORDER BY order_count DESC;
```

**金标准 SQL：**

```sql
SELECT payment_type,
       COUNT(DISTINCT order_id) AS order_count,
       ROUND(COUNT(DISTINCT order_id) * 100.0 / (SELECT COUNT(DISTINCT order_id) FROM order_payments), 2) AS pct
FROM order_payments
GROUP BY payment_type
ORDER BY order_count DESC;
```

### 17. [聚合统计] 所有订单的总运费是多少？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 3.18s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT ROUND(SUM(oi.freight_value), 2) AS total_freight
FROM order_items oi
JOIN orders o ON oi.order_id = o.order_id
WHERE o.order_status != 'canceled';
```

**金标准 SQL：**

```sql
SELECT ROUND(SUM(freight_value), 2) AS total_freight
FROM order_items;
```

### 27. [时间范围] 2017年全年一共有多少订单？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 1.51s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT COUNT(DISTINCT order_id) AS order_count
FROM orders
WHERE strftime('%Y', order_purchase_timestamp) = '2017';
```

**金标准 SQL：**

```sql
SELECT COUNT(DISTINCT order_id) AS order_count
FROM orders
WHERE order_status != 'canceled'
  AND strftime('%Y', order_purchase_timestamp) = '2017';
```

### 30. [时间范围] 平均配送时长是多少天？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 1.45s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT ROUND(AVG(julianday(order_delivered_customer_date) - julianday(order_purchase_timestamp)), 2) AS avg_delivery_days
FROM orders
WHERE order_status = 'delivered'
  AND order_delivered_customer_date IS NOT NULL
  AND order_purchase_timestamp IS NOT NULL;
```

**金标准 SQL：**

```sql
SELECT ROUND(AVG(julianday(order_delivered_customer_date)
              - julianday(order_purchase_timestamp)), 1) AS avg_delivery_days
FROM orders
WHERE order_status = 'delivered'
  AND order_delivered_customer_date IS NOT NULL;
```
