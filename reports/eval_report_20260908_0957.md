# NL2SQL 评测报告

- 评测时间：2026-09-08 09:57:31
- 评测题目：35 题
- 模型：deepseek-chat（temperature=0，失败自动重试 1 次）

## 总览

| 指标 | 数值 |
|---|---|
| 总准确率 | 71.4% (25/35) |
| 执行成功率 | 35/35 (100.0%) |
| 平均端到端耗时 | 4.26s/题 |
| 平均 LLM 调用次数 | 1.03 次/题 |

## 各类别准确率

| 类别 | 题数 | 通过 | 准确率 |
|---|---|---|---|
| 简单单表查询 | 8 | 7 | 87.5% |
| 聚合统计 | 9 | 3 | 33.3% |
| 多表JOIN | 8 | 8 | 100.0% |
| 时间范围 | 5 | 4 | 80.0% |
| 复杂组合 | 5 | 3 | 60.0% |

## 错误类型分布

| 错误类型 | 数量 |
|---|---|
| 结果不一致(语义/聚合差异,需人工复核) | 10 |

## 失败明细（供人工复核与错误类型细分）

### 1. [简单单表查询] 一共有多少个订单？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 9.03s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT COUNT(DISTINCT order_id) AS order_count
FROM orders
WHERE order_status != 'canceled';
```

**金标准 SQL：**

```sql
SELECT COUNT(DISTINCT order_id) AS order_count
FROM orders;
```

### 9. [聚合统计] 总 GMV 是多少？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 1.32s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT ROUND(SUM(price), 2) AS gmv
FROM order_items;
```

**金标准 SQL：**

```sql
SELECT ROUND(SUM(oi.price), 2) AS gmv
FROM order_items oi
JOIN orders o ON oi.order_id = o.order_id
WHERE o.order_status != 'canceled';
```

### 11. [聚合统计] 各支付方式的订单占比是多少？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 6.30s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT 
    p.payment_type,
    COUNT(DISTINCT o.order_id) AS order_count,
    ROUND(COUNT(DISTINCT o.order_id) * 100.0 / SUM(COUNT(DISTINCT o.order_id)) OVER (), 2) AS order_percentage
FROM orders o
JOIN order_payments p ON o.order_id = p.order_id
WHERE o.order_status != 'canceled'
GROUP BY p.payment_type
ORDER BY order_count DESC;
```

**金标准 SQL：**

```sql
SELECT payment_type,
       COUNT(DISTINCT order_id) AS order_count,
       ROUND(COUNT(DISTINCT order_id) * 100.0 / (SELECT COUNT(DISTINCT order_id) FROM order_payments), 2) AS pct
FROM order_payments
GROUP BY payment_type
ORDER BY order_count DESC;
```

### 12. [聚合统计] 订单评价按分数分布是怎样的？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 3.27s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT r.review_score AS score,
       COUNT(DISTINCT r.order_id) AS order_count
FROM order_reviews r
JOIN orders o ON r.order_id = o.order_id
WHERE o.order_status != 'canceled'
GROUP BY r.review_score
ORDER BY r.review_score;
```

**金标准 SQL：**

```sql
SELECT review_score,
       COUNT(*) AS review_count
FROM order_reviews
GROUP BY review_score
ORDER BY review_score;
```

### 13. [聚合统计] 哪种支付方式的订单量最多？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 5.21s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT p.payment_type,
       COUNT(DISTINCT o.order_id) AS order_count
FROM orders o
JOIN order_payments p ON o.order_id = p.order_id
WHERE o.order_status != 'canceled'
GROUP BY p.payment_type
ORDER BY order_count DESC;
```

**金标准 SQL：**

```sql
SELECT payment_type,
       COUNT(DISTINCT order_id) AS order_count
FROM order_payments
GROUP BY payment_type
ORDER BY order_count DESC
LIMIT 1;
```

### 14. [聚合统计] 客户的复购率是多少？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 5.35s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT 
    ROUND(100.0 * COUNT(CASE WHEN purchase_count >= 2 THEN 1 END) / COUNT(*), 2) AS repurchase_rate
FROM (
    SELECT c.customer_unique_id, COUNT(DISTINCT o.order_id) AS purchase_count
    FROM orders o
    JOIN customers c ON o.customer_id = c.customer_id
    WHERE o.order_status != 'canceled'
    GROUP BY c.customer_unique_id
) AS customer_purchases;
```

**金标准 SQL：**

```sql
SELECT ROUND(SUM(CASE WHEN buy_times >= 2 THEN 1 ELSE 0 END) * 100.0 / COUNT(*), 2) AS repurchase_rate_pct
FROM (SELECT c.customer_unique_id,
             COUNT(DISTINCT o.order_id) AS buy_times
      FROM customers c
      JOIN orders o ON c.customer_id = o.customer_id
      WHERE o.order_status NOT IN ('canceled', 'unavailable')
      GROUP BY c.customer_unique_id);
```

### 15. [聚合统计] 平均每个订单包含几件商品？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 2.07s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT COUNT(*) * 1.0 / COUNT(DISTINCT order_id) AS avg_items_per_order
FROM order_items;
```

**金标准 SQL：**

```sql
SELECT ROUND(AVG(cnt), 2) AS avg_items_per_order
FROM (SELECT order_id, COUNT(*) AS cnt
      FROM order_items
      GROUP BY order_id);
```

### 30. [时间范围] 平均配送时长是多少天？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 1.46s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT ROUND(AVG(julianday(order_delivered_customer_date) - julianday(order_purchase_timestamp)), 2) AS avg_delivery_days
FROM orders
WHERE order_status = 'delivered'
  AND order_delivered_customer_date IS NOT NULL
  AND order_purchase_timestamp IS NOT NULL;
```

**金标准 SQL：**

```sql
SELECT ROUND(AVG(julianday(order_delivered_customer_date)
              - julianday(order_purchase_timestamp)), 1) AS avg_delivery_days
FROM orders
WHERE order_status = 'delivered'
  AND order_delivered_customer_date IS NOT NULL;
```

### 32. [复杂组合] 在2017和2018两年都买过东西的客户有多少？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 6.23s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT COUNT(*) AS customer_count
FROM (
    SELECT c.customer_unique_id
    FROM orders o
    JOIN customers c ON o.customer_id = c.customer_id
    WHERE o.order_status != 'canceled'
      AND strftime('%Y', o.order_purchase_timestamp) IN ('2017', '2018')
    GROUP BY c.customer_unique_id
    HAVING COUNT(DISTINCT strftime('%Y', o.order_purchase_timestamp)) = 2
);
```

**金标准 SQL：**

```sql
SELECT COUNT(*) AS repeat_customers
FROM (SELECT c.customer_unique_id
      FROM orders o
      JOIN customers c ON o.customer_id = c.customer_id
      WHERE o.order_status NOT IN ('canceled', 'unavailable')
      GROUP BY c.customer_unique_id
      HAVING COUNT(DISTINCT strftime('%Y', o.order_purchase_timestamp)) >= 2);
```

### 34. [复杂组合] 取消率最高的前 5 个州是哪些？

- 结论：❌ 结果不一致(语义/聚合差异,需人工复核)（耗时 5.28s，LLM 调用 1 次）

**生成的 SQL：**

```sql
SELECT c.customer_state,
       COUNT(DISTINCT o.order_id) AS total_orders,
       SUM(CASE WHEN o.order_status = 'canceled' THEN 1 ELSE 0 END) AS canceled_orders,
       ROUND(100.0 * SUM(CASE WHEN o.order_status = 'canceled' THEN 1 ELSE 0 END) / COUNT(DISTINCT o.order_id), 2) AS cancel_rate
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id
GROUP BY c.customer_state
ORDER BY cancel_rate DESC
LIMIT 5;
```

**金标准 SQL：**

```sql
SELECT c.customer_state,
       ROUND(SUM(CASE WHEN o.order_status = 'canceled' THEN 1 ELSE 0 END) * 100.0 / COUNT(*), 2) AS cancel_rate
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id
GROUP BY c.customer_state
ORDER BY cancel_rate DESC
LIMIT 5;
```
